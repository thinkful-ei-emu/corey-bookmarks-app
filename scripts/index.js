'use strict';

//when page is loaded, used to place event listeners on page

// $(document).ready(function() {
    
//   bookmarkApp.bindEventListeners();

//   api.getBookmarks()
//     .then((bookmarksObj) => {
//       bookmarksObj.forEach((bookmark) => STORE.addBookmark(bookmark));
//       bookmarkApp.render();
//     });
// });
  